import React from "react";
import { Button } from "react-bootstrap";


const Login = (props) => {
    return (
        <Button variant="info">Login via Metamask</Button>
    )
}


export default Login;
